# TODO: Gurung Transliteration App GitHub Upload

## Remaining Steps:
1. Stage all changes: `git add .`
2. Commit changes: `git commit -m "Add Gurung transliteration app files and updates"`
3. Rename branch to main: `git branch -M main`
4. Add remote origin: `git remote add origin https://github.com/natsu-root/gurung-transliteration.git`
5. Push to GitHub: `git push -u origin main`
6. Verify on GitHub and test locally with `ng serve`

